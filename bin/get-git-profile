#!/bin/sh
name=$(git config user.name)
email=$(git config user.email)

case $name:$email in
    "valorl:11498571+valorl@users.noreply.github.com")
        echo "P"
        ;;
    "vao:valer.orlovsky@centrica.com")
        echo "W"
        ;;
    *)
        echo "U"
        ;;
esac
