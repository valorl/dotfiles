[core]
  pager = diff-so-fancy | less --tabs=4 -RFX
[user]
	name = vao
	email = valer.orlovsky@centrica.com
  ; name = vao
  ; email = valer.orlovsky@centrica.com
  #signingkey = ...
[credential]
  helper = store
#[commit]
# gpgsign = true
#[gpg]
# program = gpg
#[merge]
# tool = nvim -d
# confictstyle = diff3
[pull]
	rebase = true
