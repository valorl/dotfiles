# `valorl/dotfiles`

## TODO

- [ ] General/intro overview in this README
- [ ] Tmux workflow streamlining
    - [x] Session (and maybe window) fzf picker instead of tmux "w" ui
    - [ ] Figure out a better pattern than manual sessions per "task" with window per repo
        - [ ] Experiment?: try session per repo (primeagen sessionizer style)
- [ ] Stow-ify
- [ ] Ansible infra for dependencies (separate repo probably)




